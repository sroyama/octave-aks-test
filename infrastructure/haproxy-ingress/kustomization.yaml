apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: oct-gitops-ns

resources:
- helmrepository.yaml
- helmrelease.yaml

labels:
  - pairs:
      octave-component: haproxy-ingress
