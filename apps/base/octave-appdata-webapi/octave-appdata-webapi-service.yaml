apiVersion: v1
kind: Service
metadata:
  name: octave-appdata-webapi-service
  labels:
    app: octave-appdata-webapi
spec:
  selector:
    app: octave-appdata-webapi
  ports:
  - port: 5000
    targetPort: 5000
  type: ClusterIP
