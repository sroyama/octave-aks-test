apiVersion: v1
kind: Service
metadata:
  name: octave-shadowgram-webapi-service
  labels:
    app: octave-shadowgram-webapi
spec:
  selector:
    app: octave-shadowgram-webapi
  ports:
  - port: 5000
    targetPort: 5000
  type: ClusterIP
