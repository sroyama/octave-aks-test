apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: octave

resources:
- mongodb-pvc.yaml
- mongodb-deployment.yaml
- mongodb-service.yaml

labels:
  - pairs:
      octave-component: mongodb
