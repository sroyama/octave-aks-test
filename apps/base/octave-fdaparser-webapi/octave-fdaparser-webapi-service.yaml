apiVersion: v1
kind: Service
metadata:
  name: octave-fdaparser-webapi-service
  labels:
    app: octave-fdaparser-webapi
spec:
  selector:
    app: octave-fdaparser-webapi
  ports:
  - port: 5000
    targetPort: 5000
  type: ClusterIP
